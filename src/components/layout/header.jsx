import Navbar from '@/components/layout/navigation/navbar';

const Header = () => {
  return (
    <header className="col-span-full grid auto-cols-auto mb-4">
      <Navbar />
    </header>
  );
};

export default Header;
